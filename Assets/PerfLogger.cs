namespace DefaultNamespace
{
    public class PerfLogger
    {

        // --OVRPlugin.Ov. .Pe sPerfMetrics.System_GpuUtilPercentage_Float;

        //Start control then start test
        // SEV around performance that Pokerstars - they pulled out
        // We could define some defaults (URP default

        // --adb shell am start -n com.oclus.unitypackage
        // --apk generation in sandcastle is done and ready.

        // -- write some docs - They want their addoptions number
        // --gauntlet is where these all run - graphs is what we give devs

        // Reviews: DCP - XRX (prove system is not encroching on other services)
        // adb gpustats -g

    }
}